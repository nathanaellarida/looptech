# Development / test dependencies for the FastAPI service.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
#
# The unit tests stub the heavy OpenCV/Keras/TensorFlow runtime, so they can
# run without a GPU or the full ML stack installed.
pytest==9.1.1
pip-audit==2.7.3
